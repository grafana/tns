version: 2

workflows:
  version: 2
  test-build-deploy:
    jobs:
    - test
    - build
    - staticcheck-go
    - lint-jsonnet

defaults: &defaults
  docker:
    - image: golang:1.24.6-bullseye@sha256:2cdc80dc25edcb96ada1654f73092f2928045d037581fa4aa7c40d18af7dd85a
  working_directory: /go/src/github.com/grafana/tns

jobs:
  test:
    <<: *defaults
    steps:
      - checkout

      - run:
          name: Run Unit Tests
          command: |
            GO111MODULE=on go test ./...

  staticcheck-go:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: staticcheck-go
          command: |
            go install honnef.co/go/tools/cmd/staticcheck@latest
            staticcheck ./...

  lint-jsonnet:
    <<: *defaults
    docker:
      - image: grafana/tns-lint:cad1c20@sha256:52cb50ca5b3ee26e236ea33f45e8359dc92ed18591827807e82466e98a9d4f6b
    steps:
      - checkout
      - run:
          name: Lint Jsonnet
          command: |
            make lint-jsonnet

  build:
    <<: *defaults
    steps:
      - checkout
      - setup_remote_docker

      - run:
          name: Install Docker client
          command: |
            set -x
            VER="17.03.0-ce"
            curl -L -o /tmp/docker-$VER.tgz https://download.docker.com/linux/static/stable/x86_64/docker-$VER.tgz
            tar -xz -C /tmp -f /tmp/docker-$VER.tgz
            mv /tmp/docker/* /usr/bin

      - run:
          name: Build Images
          command: |
            make
